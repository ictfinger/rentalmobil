// src/lib/types.ts

export type Car = {
  id: number;
  brand: string;
  model: string;
  year: number;
  color: string;
  price_per_day: number;
  is_available: boolean;
  image: string;
  license_plate: string;
};
